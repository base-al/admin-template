# Frontend Configuration

# API Configuration
NUXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
NUXT_PUBLIC_API_KEY=api

# Note: In production, the frontend is served by the backend at /
# so the API base URL will be relative (/api)
